import { Card, CardBody, Heading, Text } from "@chakra-ui/react";

export default function BudgetsPage() {
  return (
    <Card>
      <CardBody>
        <Heading size="md">Orçamentos</Heading>
        <Text mt={2} color="gray.600">
          (Em construção) Lista e criação de orçamentos por OS.
        </Text>
      </CardBody>
    </Card>
  );
}
